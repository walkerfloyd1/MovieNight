import React, { Component } from 'react';

class Signin extends Component {
    render() {
        return (
            <h1>This is the sign-in landing page</h1>
        )
    }
}

export default Signin;